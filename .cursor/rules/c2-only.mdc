---
description: This repo is command and control. No product implementation here.
globs:
  - "**/*"
alwaysApply: true
---

# C2 only

- Specs, routes, status. Not robot firmware, not VLA weights, not Dash apps.
- Implement in the staffed plant, never in `campaigns/` except markdown.
- If the user says "just build the duck here," refuse and `@staff-factories`.
